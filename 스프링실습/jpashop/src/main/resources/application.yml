spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/jpashop
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create # 테이블 지우고 엔티티 확인후 다시 생성
    properties:
      hibernate:
    # show_sql: true
      format_sql: true # log

logging.level:
  org.hibernate.SQL: debug

